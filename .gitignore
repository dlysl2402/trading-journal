node_modules/

# Broker statement exports carry the account holder's name and account number.
*.xlsx

.env
.env.*
!.env.example

.DS_Store

# Left over from when this repository also drew the page. Nothing writes it
# any more — the view is `trading-journal-web` — but it carries the account's
# figures, so it stays ignored for as long as it is on disk.
equity.html

# Snapshots of the live feed: same account holder details as a statement.
data/
